import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Publish Queue — RankMaster Pro",
    description: "Manage your content publishing queue with scheduled posts, bulk actions, and WordPress auto-publishing.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
