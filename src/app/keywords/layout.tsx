import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Keyword Intelligence — RankMaster Pro",
    description: "AI-powered keyword discovery, search volume analysis, difficulty scoring, and intent classification for SEO content strategy.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
