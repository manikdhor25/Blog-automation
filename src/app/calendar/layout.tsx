import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Content Calendar — RankMaster Pro",
    description: "AI-generated content calendar with keyword-driven publishing schedule, priority levels, and progress tracking.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
