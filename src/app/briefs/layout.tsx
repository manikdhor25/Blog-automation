import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Content Briefs — RankMaster Pro",
    description: "Generate AI-powered content briefs with keyword targeting, outlines, competitor analysis, and downloadable Markdown export.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
