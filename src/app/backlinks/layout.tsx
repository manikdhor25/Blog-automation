import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Backlink Intelligence — RankMaster Pro",
    description: "Monitor backlinks, discover link building opportunities, and track domain authority across your sites.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
