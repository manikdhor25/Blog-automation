import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Image SEO & AI Generation — RankMaster Pro",
    description: "Generate AI image prompts with SEO-optimized alt text, Open Graph images, and placement suggestions.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
