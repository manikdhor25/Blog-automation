import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Rank Tracker — RankMaster Pro",
    description: "Track keyword rankings over time with position history, rank change alerts, and SERP feature monitoring.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
