import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Keyword Cannibalization — RankMaster Pro",
    description: "Detect pages competing for the same keywords with severity scoring and merge/redirect recommendations.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
