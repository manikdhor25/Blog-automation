import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Content Repurposing — RankMaster Pro",
    description: "Transform blog posts into Twitter threads, LinkedIn posts, email newsletters, and video scripts using AI.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
