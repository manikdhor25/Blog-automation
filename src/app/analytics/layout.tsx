import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Analytics Dashboard — RankMaster Pro",
    description: "Real-time analytics across sites, keywords, posts, backlinks, and rank tracking with AI-powered insights.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
