import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Programmatic SEO — RankMaster Pro",
    description: "Generate hundreds of targeted pages from templates with topic and location variables for mass SEO coverage.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
