import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Google Search Console — RankMaster Pro",
    description: "Real search traffic data with clicks, impressions, CTR, and position from Google Search Console.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
