import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Topic Clusters — RankMaster Pro",
    description: "Build topical authority with AI-powered cluster analysis, pillar content mapping, and internal linking strategy.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
