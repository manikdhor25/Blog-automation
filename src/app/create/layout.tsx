import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Content Writer — RankMaster Pro",
    description: "AI-powered content creation with competitor analysis, outline generation, and one-click WordPress publishing.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
