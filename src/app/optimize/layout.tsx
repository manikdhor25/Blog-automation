import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Content Optimizer — RankMaster Pro",
    description: "Optimize existing WordPress posts with AI-powered SEO scoring, meta tag suggestions, and one-click republishing.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
