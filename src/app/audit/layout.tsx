import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Technical SEO Audit — RankMaster Pro",
    description: "Comprehensive technical SEO audit with meta tag validation, broken link detection, and structured data checks.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
