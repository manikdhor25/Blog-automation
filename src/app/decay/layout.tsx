import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Decay Alerts — RankMaster Pro",
    description: "Detect content decay with 7 signal analysis including stale content, outdated references, and thin content alerts.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
