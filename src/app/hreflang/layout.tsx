import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Multi-Language & Hreflang — RankMaster Pro",
    description: "Manage hreflang tags and multilingual SEO across your sites. Generate proper hreflang link elements for international targeting.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
