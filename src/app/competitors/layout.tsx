import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Competitor Analysis — RankMaster Pro",
    description: "AI-powered competitor SERP analysis with domain comparison, content gap detection, and keyword overlap tracking.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
