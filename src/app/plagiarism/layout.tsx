import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "Plagiarism & AI Detection — RankMaster Pro",
    description: "Check content originality, detect AI-generated patterns, and get humanization suggestions.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
